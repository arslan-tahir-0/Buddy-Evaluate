<%# Jumbotron %>

          <%if current_user.present? %>
          <div class="jumbotron p-3 p-md-5 text-black rounded bg-light">
          <div class="col-md-6 px-0">
          <h1 class="display-4 font-italic">Hello <%= current_user.first_name %>!!</h1>
          <p class="lead my-3">

          <% if current_user.Moderator? %>
          As you are Moderator, Lets end wait for the users who are waiting for Approval of their Posts<br>
          It is essential to check the reported posts too. Have a Good day!
          <%else%>
          Welcome to Blog App, Lets share your thoughts by making a new Blog
          <%end%>

          </p>
          <%end%>
        </div>
        <% if current_user.present?%>
          <% if current_user.User?%>
            <%= button_to "Create Blog", new_blog_url, method: :get, class:"btn btn-success" %>
          <%end%>
        <%end%>
      </div>
<%# Jumbo End %>
